# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Test and coverage artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Secrets. This repo is public: the manifest signing key must never land here.
# Real values live in the environment or on the host, never in git.
.env
.env.*
!.env.example
*.pem
*.key

# macOS
.DS_Store
